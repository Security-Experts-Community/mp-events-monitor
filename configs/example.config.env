# Настройка конфигурации и аутентификации
# Файл необходимо скопировать как configs/.config.env и заполнить своими данными

# Обязательные параметры:
# HOST - FQDN MaxPatrol
HOST=your-mp-host.domain.com

# Варианты аутентификации:

# Вариант 1 (рекомендуется): Personal Token
# Личный токен для аутентификации в API
PERSONAL_TOKEN=pat_AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA

# Вариант 2: Логин/пароль
#HOST=your-mp-host.domain.com
#LOGIN=your_username
#PASSWORD=your_password

# Вариант 3: Client Secret + логин/пароль
#HOST=your-mp-host.domain.com
#LOGIN=your_username
#PASSWORD=your_password
#CLIENT_SECRET=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Дополнительные параметры (опционально):
# logging_level=INFO                # Уровень логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL
# time_delta_hours=168              # Глубина анализа событий (часы)
# reconnect_times=5                 # Повторы при запросах/ошибках
# max_uuids_in_siem_query=1000      # Количество event_src.asset в фильтрах по событиям
# max_threads_for_siem_api=11       # Количество потоков при опросе SIEM (для последовательного выполнения выставите 1)
# mode=Assets_filters               # Режим работы скрипта
# out_folder=out                    # Папка вывода результатов
# clear_mode=full                   # Режим очистки папки: full, today, day-1, day-2, not_clear
# event_policies_file=configs/event_policies.json       # Путь к файлу с политиками событий
# asset_filters_file=configs/assets_filters.json        # Путь к файлу с фильтрами активов

# Для полного списка параметров и их описания запустите: python event_checker.py -h