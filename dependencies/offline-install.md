# 💾 Офлайн установка зависимостей

Данная папка содержит архив `python-dependencies-offline.zip` с предустановленными зависимостями Python для работы в среде без доступа к интернету.

## Когда использовать

Используйте этот архив в следующих случаях:
- Отсутствует доступ к интернету
- Команда `pip install -r requirements.txt` не работает
- Необходимо быстро развернуть проект в изолированной среде
- Корпоративная среда с ограниченным доступом к внешним ресурсам

## 🛠️ Инструкция по установке

### Windows

1. **Найдите папку установки Python:**
   ```
   C:\Users\<username>\AppData\Local\Programs\Python\Python3x\
   ```

2. **Перейдите в подпапку:**
   ```
   Lib\site-packages\
   ```

3. **Распакуйте архив:**
   - Распакуйте содержимое `python-dependencies-offline.zip` в папку `site-packages`
   - Убедитесь, что все пакеты скопированы правильно

### Linux/macOS

1. **Найдите папку site-packages Python:**
   ```bash
   python -c "import site; print(site.getsitepackages())"
   ```

2. **Распакуйте архив в найденную папку:**
   ```bash
   unzip python-dependencies-offline.zip
   cp -R site-packages-offline/* /path/to/site-packages/
   ```

## ✅ Проверка установки

После установки проверьте работоспособность:

```bash
python -c "import requests, xlsxwriter, aiohttp, tqdm, pydantic, pydantic_settings, urllib3; print('Все зависимости установлены успешно')"
```

**Ожидаемый результат:** `Все зависимости установлены успешно`

## 📦 Состав архива

Архив содержит следующие пакеты и все их зависимости:

| Пакет | Версия | Назначение |
|-------|--------|------------|
| `requests` | 2.32.5 | HTTP-запросы к API MP  |
| `xlsxwriter` | 3.2.9 | Генерация Excel-отчетов |
| `aiohttp` | 3.12.15 | Асинхронные HTTP-запросы |
| `tqdm` | 4.67.1 | Прогресс-бар для asyncio |
| `pydantic` | 2.11.9 | Валидация настроек и моделей |
| `pydantic_settings` | 2.11.0 | Загрузка .env конфигурации |
| `urllib3` | 2.4.0 | HTTP-библиотека |